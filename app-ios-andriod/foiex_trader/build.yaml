targets:
  $default:
    builders:
      freezed:
        generate_for:
          include:
            - lib/features/*/data/models/*.dart
      json_serializable:
        options:
          explicit_to_json: true
        generate_for:
          include:
            - lib/features/*/data/models/*.dart
